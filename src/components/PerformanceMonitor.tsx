'use client'

// PerformanceMonitor component disabled to prevent console spam
export default function PerformanceMonitor() {
  // Component completely disabled
  return null
}

// Performance report hook for development (disabled to prevent console spam)
export function usePerformanceReport() {
  // Completely disabled to prevent console spam
}
