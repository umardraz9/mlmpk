# Production environment file for Docker build
# Real values will be injected at runtime via Fly.io secrets

# Dummy DATABASE_URL for build time (Prisma client generation)
DATABASE_URL="postgresql://dummy:dummy@dummy:5432/dummy"

# These will be overridden by Fly.io secrets at runtime
NEXTAUTH_URL="https://mlmpk-cool-shadow-1738.fly.dev"
NEXTAUTH_SECRET="will-be-overridden-by-fly-secrets"
